import type {Options} from "../../nx/options";
import type {SentenceSerial} from "../../nx/sentence";

export function parse(text: string, options: Options): void {
  // throw new ParserError('not implemented');
}
