import type {Options} from "../../nx/options";
import type {Sentence} from "../../nx/sentence";

export function generate(sent: Sentence, options: Options): void {
  // throw new GeneratorError('not implemented');
}
